# BSOD Dump Analyzer использует ТОЛЬКО стандартную библиотеку Python.
# Внешних зависимостей нет — устанавливать через pip ничего не нужно.
#
# Требуется:
#   * Python 3.8+  (модуль tkinter входит в стандартную поставку для Windows)
#
# Опционально (ставится отдельно, не через pip):
#   * Debugging Tools for Windows (cdb.exe) — для глубокого анализа !analyze -v
#   * CLI установленных ИИ: Claude Code / Codex / Grok — для анализа через ИИ
